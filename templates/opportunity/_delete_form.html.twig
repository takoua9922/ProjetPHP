<form method="post" action="{{ path('app_opportunity_delete', {'id': opportunity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opportunity.id) }}">
    <button class="btn">Delete</button>
</form>
